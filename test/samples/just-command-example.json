{
    "partials":[],
    "steps":[
        {
            "runCommand": "touch once"
        },
        {
            "runCommand": "touch {{name}}",
            "select": "$.model.types.*"
        }
    ]
}